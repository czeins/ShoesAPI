module API
  module V1
    class ShoesController < ApplicationController

        def index
          # @shoe = Shoe.find(params[:id])
          # render 'show', formats: [:json]
            end
          end
        # end

        def show
          # @shoe = Shoe.find(params[:id])
          # render @shoe, formats: [:json]
        end

    end
  end
end

